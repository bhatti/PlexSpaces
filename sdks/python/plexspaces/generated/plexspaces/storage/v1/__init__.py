# Generated by the protocol buffer compiler.  DO NOT EDIT!
# sources: plexspaces/v1/storage/blob.proto, plexspaces/v1/storage/storage_config.proto
# plugin: python-betterproto
# This file has been @generated

from dataclasses import dataclass
from datetime import (
    datetime,
    timedelta,
)
from typing import (
    TYPE_CHECKING,
    AsyncIterator,
    Dict,
    List,
    Optional,
)

import betterproto
import grpclib
from betterproto.grpc.grpclib_server import ServiceBase

from ...common import v1 as __common_v1__

if TYPE_CHECKING:
    import grpclib.server
    from betterproto.grpc.grpclib_client import MetadataLike
    from grpclib.metadata import Deadline


class StorageProvider(betterproto.Enum):
    """
    Storage provider type

     ## Purpose
     Defines the storage provider type for database backends.
     Follows Kubernetes-style provider pattern for consistency.
     Note: Message queue providers (Kafka, NATS) are defined in channel.proto.
    """

    UNSPECIFIED = 0
    POSTGRES = 1
    """Relational database providers"""

    SQLITE = 2
    REDIS = 3
    """NoSQL providers"""

    DYNAMODB = 4


@dataclass(eq=False, repr=False)
class BlobMetadata(betterproto.Message):
    """BlobMetadata represents metadata about a stored blob"""

    blob_id: str = betterproto.string_field(1)
    """Unique blob identifier (ULID or SHA256-based)"""

    tenant_id: str = betterproto.string_field(2)
    """Tenant identifier for multi-tenancy isolation"""

    namespace: str = betterproto.string_field(3)
    """Namespace within tenant (e.g., "production", "staging", "dev")"""

    name: str = betterproto.string_field(4)
    """Display name for the blob"""

    sha256: str = betterproto.string_field(5)
    """SHA256 hash of blob contents (for deduplication)"""

    content_type: str = betterproto.string_field(6)
    """Content type (MIME type)"""

    content_length: int = betterproto.int64_field(7)
    """Content length in bytes"""

    etag: str = betterproto.string_field(8)
    """ETag from object store"""

    blob_group: str = betterproto.string_field(9)
    """Blob group/category (optional grouping)"""

    kind: str = betterproto.string_field(10)
    """Blob kind/type (e.g., "LOGS", "ARTIFACTS", "CACHE", "USER_UPLOADED")"""

    metadata: Dict[str, str] = betterproto.map_field(
        11, betterproto.TYPE_STRING, betterproto.TYPE_STRING
    )
    """Custom metadata (key-value pairs)"""

    tags: Dict[str, str] = betterproto.map_field(
        12, betterproto.TYPE_STRING, betterproto.TYPE_STRING
    )
    """Custom tags (key-value pairs)"""

    expires_at: datetime = betterproto.message_field(13)
    """Expiration time (when blob should be deleted)"""

    created_at: datetime = betterproto.message_field(14)
    """Creation time"""

    updated_at: datetime = betterproto.message_field(15)
    """Last update time"""


@dataclass(eq=False, repr=False)
class BlobConfig(betterproto.Message):
    """
    Blob storage configuration

     ## Purpose
     Configuration for S3-compatible blob storage backend (S3, embedded, GCP, Azure).
     Used for storing large binary objects with metadata management.

     ## Usage
     This config is typically included in RuntimeConfig for node-level configuration,
     but can also be used standalone for service-specific configuration.
    """

    backend: str = betterproto.string_field(1)
    """
    Backend type (s3, embedded, gcp, azure)
     "embedded" uses a local S3-compatible subprocess (default for local dev/test)
    """

    bucket: str = betterproto.string_field(2)
    """Bucket name"""

    endpoint: str = betterproto.string_field(3)
    """Endpoint URL (for SeaweedFS or custom S3-compatible)"""

    region: str = betterproto.string_field(4)
    """Region (for S3/GCP/Azure)"""

    access_key_id: str = betterproto.string_field(5)
    """Access key ID"""

    secret_access_key: str = betterproto.string_field(6)
    """Secret access key (should be from env var, not config file)"""

    use_ssl: bool = betterproto.bool_field(7)
    """Use SSL/TLS"""

    prefix: str = betterproto.string_field(8)
    """Path prefix for all blobs (default: /plexspaces)"""

    gcp_service_account_json: str = betterproto.string_field(9)
    """GCP-specific: Service account JSON (base64 encoded)"""

    azure_account_name: str = betterproto.string_field(10)
    """Azure-specific: Account name"""

    azure_account_key: str = betterproto.string_field(11)
    """Azure-specific: Account key"""


@dataclass(eq=False, repr=False)
class UploadBlobRequest(betterproto.Message):
    """Upload blob request (HTTP multipart/form-data or raw body)"""

    tenant_id: str = betterproto.string_field(1)
    """Tenant ID"""

    namespace: str = betterproto.string_field(2)
    """Namespace"""

    name: str = betterproto.string_field(3)
    """Blob name"""

    content_type: str = betterproto.string_field(4)
    """Content type (optional, will be detected if not provided)"""

    blob_group: str = betterproto.string_field(5)
    """Blob group (optional)"""

    kind: str = betterproto.string_field(6)
    """Blob kind (optional)"""

    metadata: Dict[str, str] = betterproto.map_field(
        7, betterproto.TYPE_STRING, betterproto.TYPE_STRING
    )
    """Custom metadata"""

    tags: Dict[str, str] = betterproto.map_field(
        8, betterproto.TYPE_STRING, betterproto.TYPE_STRING
    )
    """Custom tags"""

    expires_after: timedelta = betterproto.message_field(9)
    """Expiration duration (optional)"""

    data: bytes = betterproto.bytes_field(10)
    """Blob data (for gRPC streaming or HTTP body)"""


@dataclass(eq=False, repr=False)
class UploadBlobResponse(betterproto.Message):
    """Upload blob response"""

    metadata: "BlobMetadata" = betterproto.message_field(1)


@dataclass(eq=False, repr=False)
class DownloadBlobRequest(betterproto.Message):
    """Download blob request"""

    blob_id: str = betterproto.string_field(1)
    """Blob ID"""

    tenant_id: str = betterproto.string_field(2)
    """Tenant ID (for authorization)"""

    namespace: str = betterproto.string_field(3)
    """Namespace (for authorization)"""


@dataclass(eq=False, repr=False)
class DownloadBlobResponse(betterproto.Message):
    """Download blob response (streaming)"""

    metadata: "BlobMetadata" = betterproto.message_field(1)
    """Blob metadata (sent first)"""

    data: bytes = betterproto.bytes_field(2)
    """Blob data chunks (streamed)"""


@dataclass(eq=False, repr=False)
class GetBlobMetadataRequest(betterproto.Message):
    """Get blob metadata request"""

    blob_id: str = betterproto.string_field(1)
    tenant_id: str = betterproto.string_field(2)
    namespace: str = betterproto.string_field(3)


@dataclass(eq=False, repr=False)
class GetBlobMetadataResponse(betterproto.Message):
    """Get blob metadata response"""

    metadata: "BlobMetadata" = betterproto.message_field(1)


@dataclass(eq=False, repr=False)
class ListBlobsRequest(betterproto.Message):
    """List blobs request"""

    tenant_id: str = betterproto.string_field(1)
    namespace: str = betterproto.string_field(2)
    name_prefix: str = betterproto.string_field(3)
    """Filter by name (prefix match)"""

    blob_group: str = betterproto.string_field(4)
    """Filter by blob_group"""

    kind: str = betterproto.string_field(5)
    """Filter by kind"""

    sha256: str = betterproto.string_field(6)
    """Filter by SHA256"""

    page: "__common_v1__.PageRequest" = betterproto.message_field(7)
    """Pagination"""


@dataclass(eq=False, repr=False)
class ListBlobsResponse(betterproto.Message):
    """List blobs response"""

    blobs: List["BlobMetadata"] = betterproto.message_field(1)
    page: "__common_v1__.PageResponse" = betterproto.message_field(2)


@dataclass(eq=False, repr=False)
class DeleteBlobRequest(betterproto.Message):
    """Delete blob request"""

    blob_id: str = betterproto.string_field(1)
    tenant_id: str = betterproto.string_field(2)
    namespace: str = betterproto.string_field(3)


@dataclass(eq=False, repr=False)
class DeleteBlobResponse(betterproto.Message):
    """Delete blob response"""

    pass


@dataclass(eq=False, repr=False)
class GeneratePresignedUrlRequest(betterproto.Message):
    """Generate presigned URL request"""

    blob_id: str = betterproto.string_field(1)
    operation: str = betterproto.string_field(2)
    """Operation: "GET" or "PUT"""

    expires_after: timedelta = betterproto.message_field(3)
    """Expiration duration (default: 1 hour)"""

    tenant_id: str = betterproto.string_field(4)
    namespace: str = betterproto.string_field(5)


@dataclass(eq=False, repr=False)
class GeneratePresignedUrlResponse(betterproto.Message):
    """Generate presigned URL response"""

    url: str = betterproto.string_field(1)
    expires_at: datetime = betterproto.message_field(2)


@dataclass(eq=False, repr=False)
class SharedDbConfig(betterproto.Message):
    """
    Shared database configuration

     ## Purpose
     Configuration for components that use a shared relational database:
     - scheduler (crates/scheduler/migrations)
     - workflow (crates/workflow/migrations)
     - object-registry (via KeyValueStore SQL backend)
     - journaling (crates/journaling/migrations)
     - blob (crates/blob/migrations)
     - locks (crates/locks/migrations)
     - channel (crates/channel/migrations)
     - keyvalue (crates/keyvalue/migrations)
     - tuplespace (crates/tuplespace/migrations)

     ## Convention
     All these components share the same database connection by default.
     Can be overridden per-component if needed.

     ## Environment Variables
     - PLEXSPACES_DATABASE_URL: Connection string (can use ${VAR} syntax)
     - PLEXSPACES_DATABASE_POOL_SIZE: Connection pool size (default: 10)
     - PLEXSPACES_DATABASE_AUTO_MIGRATE: Auto-migrate on startup (default: true)
    """

    connection_string: str = betterproto.string_field(1)
    """
    Connection string (can use environment variable: ${DATABASE_URL})
     Format: postgres://user:password@host:port/database
             sqlite:///path/to/database.db
             sqlite::memory:
    
     Security: Passwords should be in environment variables, not config files
     Example: "postgres://user:${DB_PASSWORD}@localhost:5432/plexspaces"
    """

    pool_size: int = betterproto.uint32_field(2)
    """Connection pool size (default: 10)"""

    auto_migrate: bool = betterproto.bool_field(3)
    """
    Auto-migrate on startup (default: true)
     Runs migrations from migration_paths on node startup
    """

    migration_paths: List[str] = betterproto.string_field(4)
    """
    Migration paths (default: auto-discover from crates/*/migrations)
     Paths are relative to workspace root or absolute
    """


@dataclass(eq=False, repr=False)
class RedisBackendConfig(betterproto.Message):
    """
    Redis backend configuration

     ## Purpose
     Configuration for Redis-based backends (locks, channel, keyvalue, tuplespace).
    """

    url: str = betterproto.string_field(1)
    """
    Redis connection URL
     Format: redis://[password@]host:port[/database]
             redis://localhost:6379/0
             redis://:password@redis-cluster:6379/0
    
     Security: Passwords should be in environment variables
     Example: "redis://:${REDIS_PASSWORD}@localhost:6379/0"
    """

    pool_size: int = betterproto.uint32_field(2)
    """Connection pool size (default: 10)"""

    key_prefix: str = betterproto.string_field(3)
    """
    Key prefix for namespace isolation (default: "plexspaces:")
     All keys will be prefixed with this value
    """

    connect_timeout: timedelta = betterproto.message_field(4)
    """Connection timeout (default: 5 seconds)"""

    cluster_mode: bool = betterproto.bool_field(5)
    """
    Enable cluster mode (default: false)
     If true, url should be comma-separated list of nodes
    """


@dataclass(eq=False, repr=False)
class DynamoDbBackendConfig(betterproto.Message):
    """
    DynamoDB backend configuration

     ## Purpose
     Configuration for AWS DynamoDB-based backends (locks, keyvalue, tuplespace).
    """

    region: str = betterproto.string_field(1)
    """AWS region (e.g., "us-east-1")"""

    table_prefix: str = betterproto.string_field(2)
    """
    Table name prefix (default: "plexspaces-")
     Actual table names will be: {prefix}{component_name}
    """

    endpoint_url: str = betterproto.string_field(3)
    """
    Endpoint URL (for local testing with DynamoDB Local)
     Leave empty for production (uses AWS service)
    """

    access_key_id: str = betterproto.string_field(4)
    """AWS access key ID (should be from env var, not config file)"""

    secret_access_key: str = betterproto.string_field(5)
    """AWS secret access key (should be from env var, not config file)"""


@dataclass(eq=False, repr=False)
class StorageProviderConfig(betterproto.Message):
    """
    Storage provider configuration

     ## Purpose
     Configuration for components that support multiple storage providers.
     Uses provider pattern (similar to Kubernetes StorageClass) for cleaner design.

     ## Industry Best Practices
     - Provider-based: One provider per config (not oneof with all options)
     - Explicit: Must specify provider (no magic defaults)
     - Extensible: Easy to add new providers without breaking changes

     ## Backend Selection Logic
     1. Provider must be explicitly specified (no auto-detection)
     2. If provider is UNSPECIFIED, config_manager sets defaults:
        - locks: REDIS (if available), else use shared db
     3. Validation: Fail if provider not available (feature flags)

     ## Environment Variables
     - PLEXSPACES_{COMPONENT}_PROVIDER: Provider type (e.g., "redis", "postgres")
     - Provider-specific env vars follow pattern: PLEXSPACES_{COMPONENT}_{PROVIDER}_{SETTING}
    """

    provider: "StorageProvider" = betterproto.enum_field(1)
    """Storage provider type (required)"""

    postgres: "SharedDbConfig" = betterproto.message_field(2, group="config")
    """PostgreSQL configuration (uses shared db config)"""

    sqlite: "SqliteBackendConfig" = betterproto.message_field(3, group="config")
    """SQLite configuration"""

    redis: "RedisBackendConfig" = betterproto.message_field(4, group="config")
    """Redis configuration"""

    dynamodb: "DynamoDbBackendConfig" = betterproto.message_field(5, group="config")
    """DynamoDB configuration"""


@dataclass(eq=False, repr=False)
class SqliteBackendConfig(betterproto.Message):
    """
    SQLite backend configuration

     ## Purpose
     Configuration for SQLite-based backends (primarily for channel).
    """

    database_path: str = betterproto.string_field(1)
    """
    SQLite database path
     - ":memory:" for in-memory database
     - File path for persistent database
    """

    wal_mode: bool = betterproto.bool_field(2)
    """Enable WAL mode for better concurrency (default: true)"""

    synchronous: str = betterproto.string_field(3)
    """Synchronous mode: "NORMAL", "FULL", "OFF" (default: "NORMAL")"""


@dataclass(eq=False, repr=False)
class FrameworkInfo(betterproto.Message):
    """
    Framework information

     ## Purpose
     Runtime information about the PlexSpaces framework version and build.
     Auto-populated at build time from Cargo.toml.
    """

    version: str = betterproto.string_field(1)
    """Framework version (from Cargo.toml, e.g., "0.1.0")"""

    build_date: str = betterproto.string_field(2)
    """Build date (ISO 8601 format, e.g., "2025-01-25T10:30:00Z")"""

    git_commit: str = betterproto.string_field(3)
    """Git commit hash (if available)"""

    build_target: str = betterproto.string_field(4)
    """Build target (e.g., "x86_64-unknown-linux-gnu")"""


class BlobServiceStub(betterproto.ServiceStub):
    async def upload_blob(
        self,
        upload_blob_request: "UploadBlobRequest",
        *,
        timeout: Optional[float] = None,
        deadline: Optional["Deadline"] = None,
        metadata: Optional["MetadataLike"] = None
    ) -> "UploadBlobResponse":
        return await self._unary_unary(
            "/plexspaces.storage.v1.BlobService/UploadBlob",
            upload_blob_request,
            UploadBlobResponse,
            timeout=timeout,
            deadline=deadline,
            metadata=metadata,
        )

    async def download_blob(
        self,
        download_blob_request: "DownloadBlobRequest",
        *,
        timeout: Optional[float] = None,
        deadline: Optional["Deadline"] = None,
        metadata: Optional["MetadataLike"] = None
    ) -> AsyncIterator[DownloadBlobResponse]:
        async for response in self._unary_stream(
            "/plexspaces.storage.v1.BlobService/DownloadBlob",
            download_blob_request,
            DownloadBlobResponse,
            timeout=timeout,
            deadline=deadline,
            metadata=metadata,
        ):
            yield response

    async def get_blob_metadata(
        self,
        get_blob_metadata_request: "GetBlobMetadataRequest",
        *,
        timeout: Optional[float] = None,
        deadline: Optional["Deadline"] = None,
        metadata: Optional["MetadataLike"] = None
    ) -> "GetBlobMetadataResponse":
        return await self._unary_unary(
            "/plexspaces.storage.v1.BlobService/GetBlobMetadata",
            get_blob_metadata_request,
            GetBlobMetadataResponse,
            timeout=timeout,
            deadline=deadline,
            metadata=metadata,
        )

    async def list_blobs(
        self,
        list_blobs_request: "ListBlobsRequest",
        *,
        timeout: Optional[float] = None,
        deadline: Optional["Deadline"] = None,
        metadata: Optional["MetadataLike"] = None
    ) -> "ListBlobsResponse":
        return await self._unary_unary(
            "/plexspaces.storage.v1.BlobService/ListBlobs",
            list_blobs_request,
            ListBlobsResponse,
            timeout=timeout,
            deadline=deadline,
            metadata=metadata,
        )

    async def delete_blob(
        self,
        delete_blob_request: "DeleteBlobRequest",
        *,
        timeout: Optional[float] = None,
        deadline: Optional["Deadline"] = None,
        metadata: Optional["MetadataLike"] = None
    ) -> "DeleteBlobResponse":
        return await self._unary_unary(
            "/plexspaces.storage.v1.BlobService/DeleteBlob",
            delete_blob_request,
            DeleteBlobResponse,
            timeout=timeout,
            deadline=deadline,
            metadata=metadata,
        )

    async def generate_presigned_url(
        self,
        generate_presigned_url_request: "GeneratePresignedUrlRequest",
        *,
        timeout: Optional[float] = None,
        deadline: Optional["Deadline"] = None,
        metadata: Optional["MetadataLike"] = None
    ) -> "GeneratePresignedUrlResponse":
        return await self._unary_unary(
            "/plexspaces.storage.v1.BlobService/GeneratePresignedUrl",
            generate_presigned_url_request,
            GeneratePresignedUrlResponse,
            timeout=timeout,
            deadline=deadline,
            metadata=metadata,
        )


class BlobServiceBase(ServiceBase):

    async def upload_blob(
        self, upload_blob_request: "UploadBlobRequest"
    ) -> "UploadBlobResponse":
        raise grpclib.GRPCError(grpclib.const.Status.UNIMPLEMENTED)

    async def download_blob(
        self, download_blob_request: "DownloadBlobRequest"
    ) -> AsyncIterator[DownloadBlobResponse]:
        raise grpclib.GRPCError(grpclib.const.Status.UNIMPLEMENTED)
        yield DownloadBlobResponse()

    async def get_blob_metadata(
        self, get_blob_metadata_request: "GetBlobMetadataRequest"
    ) -> "GetBlobMetadataResponse":
        raise grpclib.GRPCError(grpclib.const.Status.UNIMPLEMENTED)

    async def list_blobs(
        self, list_blobs_request: "ListBlobsRequest"
    ) -> "ListBlobsResponse":
        raise grpclib.GRPCError(grpclib.const.Status.UNIMPLEMENTED)

    async def delete_blob(
        self, delete_blob_request: "DeleteBlobRequest"
    ) -> "DeleteBlobResponse":
        raise grpclib.GRPCError(grpclib.const.Status.UNIMPLEMENTED)

    async def generate_presigned_url(
        self, generate_presigned_url_request: "GeneratePresignedUrlRequest"
    ) -> "GeneratePresignedUrlResponse":
        raise grpclib.GRPCError(grpclib.const.Status.UNIMPLEMENTED)

    async def __rpc_upload_blob(
        self, stream: "grpclib.server.Stream[UploadBlobRequest, UploadBlobResponse]"
    ) -> None:
        request = await stream.recv_message()
        response = await self.upload_blob(request)
        await stream.send_message(response)

    async def __rpc_download_blob(
        self, stream: "grpclib.server.Stream[DownloadBlobRequest, DownloadBlobResponse]"
    ) -> None:
        request = await stream.recv_message()
        await self._call_rpc_handler_server_stream(
            self.download_blob,
            stream,
            request,
        )

    async def __rpc_get_blob_metadata(
        self,
        stream: "grpclib.server.Stream[GetBlobMetadataRequest, GetBlobMetadataResponse]",
    ) -> None:
        request = await stream.recv_message()
        response = await self.get_blob_metadata(request)
        await stream.send_message(response)

    async def __rpc_list_blobs(
        self, stream: "grpclib.server.Stream[ListBlobsRequest, ListBlobsResponse]"
    ) -> None:
        request = await stream.recv_message()
        response = await self.list_blobs(request)
        await stream.send_message(response)

    async def __rpc_delete_blob(
        self, stream: "grpclib.server.Stream[DeleteBlobRequest, DeleteBlobResponse]"
    ) -> None:
        request = await stream.recv_message()
        response = await self.delete_blob(request)
        await stream.send_message(response)

    async def __rpc_generate_presigned_url(
        self,
        stream: "grpclib.server.Stream[GeneratePresignedUrlRequest, GeneratePresignedUrlResponse]",
    ) -> None:
        request = await stream.recv_message()
        response = await self.generate_presigned_url(request)
        await stream.send_message(response)

    def __mapping__(self) -> Dict[str, grpclib.const.Handler]:
        return {
            "/plexspaces.storage.v1.BlobService/UploadBlob": grpclib.const.Handler(
                self.__rpc_upload_blob,
                grpclib.const.Cardinality.UNARY_UNARY,
                UploadBlobRequest,
                UploadBlobResponse,
            ),
            "/plexspaces.storage.v1.BlobService/DownloadBlob": grpclib.const.Handler(
                self.__rpc_download_blob,
                grpclib.const.Cardinality.UNARY_STREAM,
                DownloadBlobRequest,
                DownloadBlobResponse,
            ),
            "/plexspaces.storage.v1.BlobService/GetBlobMetadata": grpclib.const.Handler(
                self.__rpc_get_blob_metadata,
                grpclib.const.Cardinality.UNARY_UNARY,
                GetBlobMetadataRequest,
                GetBlobMetadataResponse,
            ),
            "/plexspaces.storage.v1.BlobService/ListBlobs": grpclib.const.Handler(
                self.__rpc_list_blobs,
                grpclib.const.Cardinality.UNARY_UNARY,
                ListBlobsRequest,
                ListBlobsResponse,
            ),
            "/plexspaces.storage.v1.BlobService/DeleteBlob": grpclib.const.Handler(
                self.__rpc_delete_blob,
                grpclib.const.Cardinality.UNARY_UNARY,
                DeleteBlobRequest,
                DeleteBlobResponse,
            ),
            "/plexspaces.storage.v1.BlobService/GeneratePresignedUrl": grpclib.const.Handler(
                self.__rpc_generate_presigned_url,
                grpclib.const.Cardinality.UNARY_UNARY,
                GeneratePresignedUrlRequest,
                GeneratePresignedUrlResponse,
            ),
        }
