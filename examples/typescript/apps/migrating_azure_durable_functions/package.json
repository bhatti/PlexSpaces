{
  "name": "migrating-azure-durable-document-processing",
  "version": "0.1.0",
  "description": "Azure Durable Functions \u2192 PlexSpaces: Document processing workflow (TypeScript WASM)",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "npm run build:ts && npm run build:bundle",
    "build:ts": "tsc -p .",
    "build:bundle": "node build-bundle.mjs",
    "test": "npm run build",
    "verify": "npm run build && echo 'Run ./test.sh 8091 with server (./scripts/server.sh) for e2e'"
  },
  "dependencies": {
    "@plexspaces/sdk": "^0.1"
  },
  "devDependencies": {
    "@bytecodealliance/componentize-js": "^0.14.0",
    "@bytecodealliance/jco": "^1.16.0",
    "esbuild": "^0.24.0",
    "typescript": "^5.0.0"
  }
}
