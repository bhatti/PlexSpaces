{
  "name": "migrating-cloudflare-workers-guild-chat",
  "version": "0.1.0",
  "description": "Cloudflare Workers Durable Objects \u2192 PlexSpaces: Discord-style guild chat (TypeScript WASM)",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "npm run build:ts && npm run build:bundle",
    "build:ts": "tsc -p .",
    "build:bundle": "node build-bundle.mjs",
    "test": "node verify.mjs",
    "verify": "npm run build && npm test"
  },
  "dependencies": {
    "@plexspaces/sdk": "file:../../../../sdks/typescript"
  },
  "devDependencies": {
    "@bytecodealliance/componentize-js": "^0.14.0",
    "@bytecodealliance/jco": "^1.16.0",
    "esbuild": "^0.24.0",
    "typescript": "^5.0.0"
  }
}
