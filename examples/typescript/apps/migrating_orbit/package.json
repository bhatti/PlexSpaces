{
  "name": "migrating-orbit-read-state",
  "version": "0.1.0",
  "description": "Orbit → PlexSpaces: Discord-style read state tracker (TypeScript WASM)",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "npm run build:ts && npm run build:bundle",
    "build:ts": "tsc -p .",
    "build:bundle": "node build-bundle.mjs",
    "test": "node verify.mjs",
    "verify": "npm run build && npm test"
  },
  "dependencies": {
    "@plexspaces/sdk": "file:../../../../sdks/typescript"
  },
  "devDependencies": {
    "@bytecodealliance/componentize-js": "^0.14.0",
    "@bytecodealliance/jco": "^1.16.0",
    "esbuild": "^0.24.0",
    "typescript": "^5.0.0"
  }
}
