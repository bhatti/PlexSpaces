version: '3.8'

services:
  postgres:
    image: postgres:15-alpine
    environment:
      POSTGRES_DB: plexspaces
      POSTGRES_USER: plexspaces
      POSTGRES_PASSWORD: password
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data
      - ./scripts/init.sql:/docker-entrypoint-initdb.d/init.sql

  mongodb:
    image: mongo:7-jammy
    ports:
      - "27017:27017"
    volumes:
      - mongodb_data:/data/db

  plexspaces-server:
    build: .
    ports:
      - "50051:50051"
      - "8080:8080"
    depends_on:
      - postgres
      - mongodb
    environment:
      - DATABASE_URL=postgresql://plexspaces:password@postgres:5432/plexspaces
      - MONGODB_URL=mongodb://mongodb:27017
      - LOCKS_BACKEND=postgres
      - LOCKS_POSTGRES_URL=postgresql://plexspaces:password@postgres:5432/plexspaces
      - CHANNEL_BACKEND=sqlite
      - CHANNEL_SQLITE_PATH=:memory:
      - RUST_LOG=debug
    volumes:
      - ./config:/app/config
      - /var/run/docker.sock:/var/run/docker.sock

volumes:
  postgres_data:
  mongodb_data:
