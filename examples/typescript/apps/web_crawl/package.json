{
  "name": "plexspaces-web-crawl-example",
  "version": "0.1.0",
  "description": "PlexSpaces web crawler example (TypeScript WASM) \u2014 ElasticPool + TupleSpace + ShardGroup",
  "private": true,
  "type": "module",
  "license": "AGPL-3.0-or-later",
  "scripts": {
    "build": "npm run build:ts && npm run build:bundle",
    "build:ts": "tsc -p .",
    "build:bundle": "node build-bundle.mjs"
  },
  "dependencies": {
    "@plexspaces/sdk": "^0.1"
  },
  "devDependencies": {
    "@bytecodealliance/componentize-js": "^0.14.0",
    "@bytecodealliance/jco": "^1.16.0",
    "esbuild": "^0.24.0",
    "typescript": "^5.0.0"
  }
}
