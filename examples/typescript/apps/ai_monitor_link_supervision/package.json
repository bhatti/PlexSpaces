{
  "name": "ai-monitor-link-supervision-ts",
  "version": "0.1.0",
  "description": "AI Monitor/Link Supervision: FLP/Byzantine fault detection with actor monitor/link primitives (TypeScript WASM)",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "npm run build:ts && npm run build:bundle",
    "build:ts": "tsc -p .",
    "build:bundle": "node build-bundle.mjs"
  },
  "dependencies": {
    "@plexspaces/sdk": "^0.1"
  },
  "devDependencies": {
    "@bytecodealliance/componentize-js": "^0.14.0",
    "@bytecodealliance/jco": "^1.17.2",
    "esbuild": "^0.24.2",
    "typescript": "^5.0.0"
  }
}
