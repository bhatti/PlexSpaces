{
  "name": "plexspaces-abstractions-example",
  "version": "0.1.0",
  "description": "PlexSpaces abstractions example (TypeScript WASM)",
  "private": true,
  "type": "module",
  "license": "AGPL-3.0-or-later",
  "scripts": {
    "build": "npm run build:ts && npm run build:bundle",
    "build:ts": "tsc -p .",
    "build:bundle": "node build-bundle.mjs"
  },
  "dependencies": {
    "@esbuild/darwin-arm64": "^0.24.2",
    "@plexspaces/sdk": "^0.1"
  },
  "devDependencies": {
    "@bytecodealliance/componentize-js": "^0.14.0",
    "@bytecodealliance/jco": "^1.16.0",
    "esbuild": "^0.24.0",
    "typescript": "^5.0.0"
  }
}
