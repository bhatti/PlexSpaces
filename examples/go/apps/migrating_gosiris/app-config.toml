# Gosiris IoT Sensor Aggregation Application Configuration
#
# Deploys a sensor network with multiple sensor actors and an aggregator.
# Demonstrates Gosiris-style actor model with process groups for discovery.
#
# Architecture:
#   sensor-* actors -> join "sensors" process group -> aggregator polls via PG

tenant_id = "default"

[supervisor]
strategy = "one_for_one"
max_restarts = 10
max_restart_window_seconds = 60

# Aggregator actor - collects and analyzes sensor data
[[supervisor.children]]
name = "aggregator"
tenant_id = "default"
actor_type = "migrating_gosiris_wasm"
role = "aggregator"
restart = "permanent"
shutdown_timeout_seconds = 10

[supervisor.children.args]
max_readings = "200"
alert_thresh_temp_c = "5.0"
alert_thresh_humid = "20.0"

# Sensor 1 - Data center zone A (cool)
[[supervisor.children]]
name = "sensor-dc-zone-a"
actor_type = "migrating_gosiris_wasm"
role = "sensor-dc-zone-a"
restart = "permanent"
shutdown_timeout_seconds = 5

[supervisor.children.args]
location = "dc-zone-a"
sensor_type = "temp_humidity"
base_temp_c = "18.5"
base_humidity = "35.0"

# Sensor 2 - Data center zone B (warm)
[[supervisor.children]]
name = "sensor-dc-zone-b"
actor_type = "migrating_gosiris_wasm"
role = "sensor-dc-zone-b"
restart = "permanent"
shutdown_timeout_seconds = 5

[supervisor.children.args]
location = "dc-zone-b"
sensor_type = "temp_humidity"
base_temp_c = "24.0"
base_humidity = "42.0"

# Sensor 3 - Server room (hot)
[[supervisor.children]]
name = "sensor-server-room"
actor_type = "migrating_gosiris_wasm"
role = "sensor-server-room"
restart = "permanent"
shutdown_timeout_seconds = 5

[supervisor.children.args]
location = "server-room"
sensor_type = "temp_humidity"
base_temp_c = "28.5"
base_humidity = "30.0"

# Sensor 4 - Outdoor (variable)
[[supervisor.children]]
name = "sensor-outdoor"
actor_type = "migrating_gosiris_wasm"
role = "sensor-outdoor"
restart = "permanent"
shutdown_timeout_seconds = 5

[supervisor.children.args]
location = "outdoor"
sensor_type = "temp_humidity"
base_temp_c = "15.0"
base_humidity = "65.0"
tenant_id = "default"
