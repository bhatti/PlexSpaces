# Load Test Report

<!-- Fill in the blanks after running: bash load-test/k6/run.sh -->

**Run ID**: <!-- e.g. 20260724-143000 -->
**Date**: <!-- e.g. 2026-07-24 -->
**Mode**: <!-- echo | kv | spawn | pg | shard | pool | capacity | all -->
**Concurrent users (VUs)**: <!-- 100 -->
**Iterations per VU**: <!-- 100 -->

---

## Environment

| Field | Value |
|-------|-------|
| OS | <!-- macOS 15 / Ubuntu 24.04 --> |
| CPU | <!-- Apple M3 Pro / AMD EPYC 8-core --> |
| Memory limit | <!-- 1G / 2G --> |
| Server version | <!-- git commit hash --> |
| k6 version | <!-- k6 0.xx --> |

---

## Echo — Transport Comparison (operation: echo, all languages combined)

> Goal: gRPC should be faster than HTTP. If HTTP is faster, investigate the gRPC handler path.

| Transport | p50 (ms) | p95 (ms) | p99 (ms) | RPS | Errors |
|-----------|---------|---------|---------|-----|--------|
| HTTP/REST |         |         |         |     |        |
| gRPC      |         |         |         |     |        |

---

## Echo — Language Comparison (transport: HTTP)

> Goal: Rust Embedded should be fastest. Rust WASM ≈ 2-5x slower. Go/TS ≈ 5-10x. Python ≈ slowest.
> If Rust WASM is slower than expected, look at WASM module load time and single-thread dispatch.

| Language | p50 (ms) | p95 (ms) | p99 (ms) | RPS | Errors |
|----------|---------|---------|---------|-----|--------|
| Rust Embedded (baseline) | | | | | |
| Rust WASM | | | | | |
| Go WASM | | | | | |
| TypeScript WASM | | | | | |
| Python WASM | | | | | |

---

## Echo — Language Comparison (transport: gRPC)

| Language | p50 (ms) | p95 (ms) | p99 (ms) | RPS | Errors |
|----------|---------|---------|---------|-----|--------|
| Rust Embedded (baseline) | | | | | |
| Rust WASM | | | | | |
| Go WASM | | | | | |
| TypeScript WASM | | | | | |
| Python WASM | | | | | |

---

## KV — put+get Round-Trip

| Transport | Language | p50 (ms) | p95 (ms) | p99 (ms) | RPS |
|-----------|----------|---------|---------|---------|-----|
| HTTP | Rust Embedded | | | | |
| HTTP | Python WASM | | | | |
| gRPC | Rust Embedded | | | | |
| gRPC | Python WASM | | | | |

> SQLite write latency is usually the dominant factor here. If p99 > 500ms, check disk I/O.

---

## Actor Groups

### ShardGroup (HPC scatter-gather)

| Shard count | Scatter-gather p50 | Scatter-gather p99 | All-reduce p50 | Errors |
|-------------|-------------------|-------------------|---------------|--------|
| 500 | | | | |
| 1000 | | | | |
| 2000 | | | | |

> If scatter-gather time grows faster than linearly with shard count, the bottleneck
> is in the coordinator aggregation path, not the shard compute.

### ProcessGroup (pub/sub broadcast)

| Members | Broadcast p50 | Broadcast p99 | Errors |
|---------|--------------|--------------|--------|
| 500 | | | |
| 1000 | | | |
| 2000 | | | |

### ElasticPool (checkout/compute/checkin)

| Metric | Value |
|--------|-------|
| Checkout p50 | |
| Checkout p99 | |
| Compute p50 | |
| Pool scale events | |
| Peak utilization | |

---

## Actor Capacity

| Memory limit | Max actors | Overhead per actor | Erlang reference |
|-------------|-----------|-------------------|------------------|
| 1 GB | | KB | ~0.3 KB |
| 2 GB | | KB | ~0.3 KB |

> If overhead > 50 KB/actor, investigate: Tokio task stack size, mailbox buffer allocation,
> actor struct size, and whether virtual actor idle-timeout is flushing state.

---

## System Resources at Peak Load

| Metric | Value |
|--------|-------|
| RSS (peak) | MB |
| CPU (peak) | % |
| Max mailbox depth | messages |
| SQLite write latency | ms |
| gRPC active streams | |
| Error rate | % |

---

## Bottlenecks Found This Run

<!-- Fill in after reviewing the results. Be specific: name the metric and the code path. -->

1. **[Bottleneck name]**: [Plain description of what is slow and by how much]
   - Evidence: p99 latency = Xms at VUS=Y, which is Zx higher than baseline
   - Likely cause: [e.g., WASM single-thread dispatch, SQLite fsync, Tokio thread pool saturation]
   - Suggested fix: [e.g., increase Tokio worker threads, batch SQLite writes, enable WAL mode]
   - Priority: High / Medium / Low

---

## Flamegraph Summary

> Attach or link the SVG files generated by the profiler.

- CPU flamegraph: `reports/<run-id>/flamegraph-cpu.svg`
- Heap flamegraph: `reports/<run-id>/flamegraph-heap.svg`

**Top 5 hot functions (from CPU flamegraph)**:

1.
2.
3.
4.
5.

---

## Regression vs Previous Run

> Fill in after running: bash load-test/reports/compare.sh <previous> <this>

| Metric | Previous | This run | Change |
|--------|----------|----------|--------|
| echo HTTP p99 (rust-embedded) | | | |
| echo gRPC p99 (rust-embedded) | | | |
| echo HTTP p99 (python wasm) | | | |
| Actor capacity 1G | | | |

---

## Next Steps

- [ ] Fix bottleneck #1: [brief description]
- [ ] Re-run echo at 1000 VUs after fix to confirm improvement
- [ ] Investigate [specific area] using flamegraph
- [ ] Run soak test (30min at 500 VUs) after fixes
