# Generated by the protocol buffer compiler.  DO NOT EDIT!
# sources: plexspaces/prv/locks/locks.proto
# plugin: python-betterproto
# This file has been @generated

from dataclasses import dataclass
from datetime import datetime
from typing import Dict

import betterproto


@dataclass(eq=False, repr=False)
class Lock(betterproto.Message):
    """Distributed lock/lease for coordination"""

    lock_key: str = betterproto.string_field(1)
    """Lock key (unique identifier)"""

    holder_id: str = betterproto.string_field(2)
    """
    Node/process ID holding the lock (only this identity can renew/release)
    """

    version: str = betterproto.string_field(3)
    """
    Lock version (for optimistic locking)
     Format: ULID or UUID (monotonically increasing for ordering)
    """

    expires_at: datetime = betterproto.message_field(4)
    """Lock expiration timestamp"""

    lease_duration_secs: int = betterproto.uint32_field(5)
    """Lease duration (seconds)"""

    last_heartbeat: datetime = betterproto.message_field(6)
    """Last heartbeat timestamp"""

    metadata: Dict[str, str] = betterproto.map_field(
        7, betterproto.TYPE_STRING, betterproto.TYPE_STRING
    )
    """Lock metadata (optional)"""

    locked: bool = betterproto.bool_field(8)
    """Whether lock is currently held"""


@dataclass(eq=False, repr=False)
class AcquireLockOptions(betterproto.Message):
    """Lock acquisition options"""

    lock_key: str = betterproto.string_field(1)
    """Lock key (unique identifier)"""

    holder_id: str = betterproto.string_field(2)
    """
    Holder ID (node/process ID requesting the lock; only this identity can renew/release)
    """

    lease_duration_secs: int = betterproto.uint32_field(3)
    """Lease duration (seconds)"""

    additional_wait_time_ms: int = betterproto.uint32_field(4)
    """
    Total wait time for acquisition (milliseconds)
     0 = try once, no retry
    """

    refresh_period_ms: int = betterproto.uint32_field(5)
    """
    Polling interval for retry (milliseconds)
     Used when additional_wait_time_ms > 0
    """

    metadata: Dict[str, str] = betterproto.map_field(
        6, betterproto.TYPE_STRING, betterproto.TYPE_STRING
    )
    """Lock metadata (optional)"""


@dataclass(eq=False, repr=False)
class RenewLockOptions(betterproto.Message):
    """Lock renewal options (heartbeat)"""

    lock_key: str = betterproto.string_field(1)
    """Lock key (unique identifier)"""

    holder_id: str = betterproto.string_field(2)
    """Holder ID (must match current holder to renew)"""

    version: str = betterproto.string_field(3)
    """Current version (must match for optimistic locking)"""

    lease_duration_secs: int = betterproto.uint32_field(4)
    """Lease duration (seconds) - extends expiration"""

    metadata: Dict[str, str] = betterproto.map_field(
        5, betterproto.TYPE_STRING, betterproto.TYPE_STRING
    )
    """Lock metadata (optional, updates existing)"""


@dataclass(eq=False, repr=False)
class ReleaseLockOptions(betterproto.Message):
    """Lock release options"""

    lock_key: str = betterproto.string_field(1)
    """Lock key (unique identifier)"""

    holder_id: str = betterproto.string_field(2)
    """Holder ID (must match current holder to release)"""

    version: str = betterproto.string_field(3)
    """Current version (must match for optimistic locking)"""

    delete_lock: bool = betterproto.bool_field(4)
    """
    Delete lock entry after release
     false = keep entry (for audit trail)
     true = remove entry completely
    """
